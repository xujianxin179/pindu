# pinDu

一个自用的拼豆设计 iPad 应用：导入图片自动转成拼豆图案，实时算色，生成图纸并可分享。

## Language

**色板 (ColorPalette)**:
一组带色号的珠子颜色，是图片量化和算色的基准。本项目采用 MARD 221 色号体系。
_Avoid_: 调色板, 颜色表

**用色集 (Active Palette)**:
一次图片转图案中实际选用的色号子集。量化时从色板的 221 色里按"最能还原本图"选出（大小受用色数上限约束）；编辑时可手动引入用色集之外的色板色号，用色集随之扩展。
_Avoid_: 子色板, 实际色板, used colors

**图案 (Pattern)**:
一个像素网格，每格对应一颗珠子的颜色。尺寸由用户自由设定（可调节网格），不绑定标准钉板。是"图片转图案"的产物，也是算色、生成图纸、分享的对象。
_Avoid_: 图, 设计稿, design

**作品 (Work)**:
一个已保存的图案实例及其元信息（名称、创建/修改时间、原图缩略图），是用户在作品库中管理的单元。
_Avoid_: 项目, 文件, project

**算色 (Color Counting)**:
统计一个图案里每种色号各需要多少颗珠子。
_Avoid_: 统计, 计数, 颜色统计

**图纸 (Build Sheet)**:
照着拼装用的指引，含带辅助线的图案预览和用色清单（色号、样块、数量）。可导出图片和 PDF。
_Avoid_: 导出图, 输出, export
